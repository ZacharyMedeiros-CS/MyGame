


console.log('Hello World');

// To draw on the canvas, you'll need to access its rendering context.
// The getContext("2d") method returns a 2D rendering context. This
// context provides methods and properties for drawing shapes, text, 
// and images on the canvas.

/*
const canvas = document.getElementById("gameCanvas");
const context = canvas.getContext("2d");

context.fillStyle = "blue";
context.fillRect(50, 50, 100, 100);
*/